import React from 'react'
import { api } from 'xadmin'
import _ from 'lodash'
import locales from '../../locales'

export default {
  name: 'iot-ftp-setting',
  locales,
  'settings.schema': app => (props, cb) => {
    cb(null, {
      name: 'ftpSettings',
      title: _r('ftp配置'),
      parentIndex: 'modular',
      type: 'object',
      properties: {
        username: {
          title: _r('用户名'),
          type: 'string'
        },
        password: {
          title: _r('密码'),
          type: 'string'
        },
        host: {
          title: _r('主机'),
          type: 'string'
        },
        port: {
          title: _r('端口'),
          type: 'string'
        }
      },
      required: [ 'host' ],
      form: [ 'username', 'password', 'host', 'port' ]
    })
  }
}
