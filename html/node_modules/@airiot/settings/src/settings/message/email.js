import React from 'react'
import locales from '../../../locales'
// import NumberFormat from 'react-number-format'

// const PhoneFormat = props => {
//   return <NumberFormat format="###-####-####" mask="_" />
// }

export default {
  name: 'iot-email',
  'settings.schema': app => (props, cb) => {
    cb(null, {
      name: 'email',
      parentTab: _r('通知服务配置'),
      title: _r('邮箱服务'),
      type: 'object',
      properties: {
        host: {
          type: 'string',
          title: _r('主机')
        },
        port: {
          type: 'number',
          title: _r('端口')
        },
        from: {
          type: 'string',
          title: _r('邮箱'),
          format: 'email'
        },
        password: {
          type: 'string',
          title: _r('密码'),
          fieldType: 'password',
        },
      },
      form: ['host', 'port', 'from', 'password'],
      required: ['host', 'port', 'from', 'password']
    })
  }
}
