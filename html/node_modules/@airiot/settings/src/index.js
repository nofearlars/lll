import React from 'react'
import { app } from 'xadmin';
import ftpSetting from './settings/ftpSetting'
import onlineSetting from './settings/onlineSetting'
import NodeConfig from './settings/NodeConfig'
import userExpand from './settings/userExpand'
import { dingtalk, email, wechat, wechatEE, note } from './settings/message'
import setting from './setting'
import dashboardconfig from './settings/dashboardconfig'
import system from './system';
import languageSettings from "./component/LanguagePage"
export default app
  .use(system)
  .use(NodeConfig)
  .use(ftpSetting)
  .use(userExpand)
  .use(dingtalk)
  .use(email)
  .use(note)
  .use(wechat)
  .use(wechatEE)
  .use(dashboardconfig)
  .use(onlineSetting)
  .use(setting)
  .use(languageSettings)
