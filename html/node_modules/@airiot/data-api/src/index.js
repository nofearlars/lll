import { app } from 'xadmin';
import models from './models'
import Main from './Main'
import dashboardDataSource from './dataset'
import locales from '../locales'
import AIalgorithm from './AIalgorithm';

export default app.use({
  name: 'iot.datasource',
  models,
  locales,
  routers: {
    '/app/': [
      { path: 'ds', component: Main },
    ]
  },
  icons: [
    { name: 'DataApi', url: require('./icons/流程管理.svg') },
  ],
  menuItems: [
    { title: _r('数据接口'), key: 'dataApi', icon: 'DataApi', url: '/app/ds', permission: 'group.view' }
  ],
  dashboardDataSource
}).use(AIalgorithm)