import _ from 'lodash';

const useTableId = (table) => {
  if (_.isString(table)) {
    return table
  } else if (_.isPlainObject(table)) {
    return table.id
  } else if (_.isArray(table)) {
    return table.map(item => item?.id || item)
  }
  return null
}

export default useTableId