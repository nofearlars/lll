import React from 'react'
import { api, use } from 'xadmin'
import { TreeSelect, Select } from 'antd'

const SelectLineRecord = () => {
    const fetchReacord = () => {
        
    }
    return <SelectRecord></SelectRecord>
}

const SelectRecord = () => {
    const { table, input, multiple, tree } = props

    // 兼容多选空值
    React.useEffect(() => {
        if (multiple && !input?.value) {
        input?.onChange && input.onChange([])
        }
    }, [input?.value, multiple])
    // 指定单表
    if (_.isString(table)) {
        return <SelectByTableName {...props} ></SelectByTableName>
        // 指定多表或不指定
    } else {
        return <SelectAllTable {...props} ></SelectAllTable>
    }
}

export default SelectRecord
