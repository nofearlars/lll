import { Menu } from 'antd';
import React from 'react';
import { config as _c, Block } from 'xadmin'

const UserMenuComponent = props => {

  return (
    <>
      <Block name="top.right" >
        {items => {
          return <Menu style={{ maxWidth: 418, minWidth: 240 }} className="top-menu" mode="horizontal">{items}</Menu>
        }}
      </Block>
    </>
  )
}

const paramSchema = {
  type: 'object',
  properties: {
    color: {
      title: _r('颜色'),
      type: 'string',
      fieldType: 'color'
    },
  }
}

const UserMenu = {
  title: _r('用户组'),
  category: ['页面元素', '导航栏'],
  icon: require('../icons/用户组.svg'),
  component: UserMenuComponent,
  initLayout: { width: 260, height: 60 },
  paramSchema,
  theme: [
    { selector: '.top-menu', title: _r('用户组外层样式') },
    { selector: '.top-menu .nav-text', title: _r('用户组样式') }
  ]
}
export default UserMenu
