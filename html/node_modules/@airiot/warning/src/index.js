import app from 'xadmin'
import warningPlugins from './plugins/warning'
import warning from './apps/warning'
import Rule from './apps/warning/model_rule'
import WarningArchive from './apps/warning/model_archive'
import tRecord from './apps/warning/model_record'

//  后端去除blocks
// warning.blocks.root = () => null
// warning.blocks.main = () => null

export default app
  .use(warning)
  .use(warningPlugins)
  .use({ models: Rule })
  .use({ models: WarningArchive })
  .use({ models: tRecord })
