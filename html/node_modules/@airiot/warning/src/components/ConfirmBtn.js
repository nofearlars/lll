import React from 'react';
import { Button, Popconfirm } from 'antd';
import { use } from 'xadmin';

// @StoreWrap('warnings.confirmall')
const ConfirmBtn = props => {  
  const [ confirmAll, setConfirmAll ] = use('warnings.confirmall')
  const { t:_t1, i18n } = useI18n()

  const confirm = () => {
    // this.props.confirmAll()
    setConfirmAll({})
  }
  
  return (
    <Popconfirm title={_t1("您确定要将全部报警信息确认？")} onConfirm={confirm} okText={_t1("确认全部报警信息")} cancelText={_t1("取消")}>
      <Button type="danger" block>
        {_t1('全部确认')}
      </Button>
    </Popconfirm>
  )
}

export default ConfirmBtn
