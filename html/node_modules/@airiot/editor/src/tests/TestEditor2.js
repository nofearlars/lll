import { Button } from 'antd';
import React, { Component } from 'react';
import { Page, C } from 'xadmin-ui'
import sqlBuilder from 'raw-loader!./react.d.ts'

const code = 
`#bac.class {
  background: #fff;
}
`

const code2 = 
`let a = '123';
let b = () => {
  return 'OK'
}
Math.pow(2, 3);
`

const App = () => {
  const [ value, setValue ] = React.useState(code2)
  return (
    <Page title="代码编辑器">
      <Button onClick={() => setValue('#____')}>点击测试</Button>
      <C is="CodeEditor"
        width="100%"
        height={500}
        language="javascript"
        value={value}
        editorDidMount={(editor, monaco) => {
          monaco.languages.typescript.javascriptDefaults.addExtraLib(sqlBuilder)
        }}
        defaultValue={`/** 这里要填写CSS **/`}
        onChange={console.log}
      />
    </Page>
  )
};

export default App;
