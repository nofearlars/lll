/**
 * 返回一个数据点的最新值.
 * 
 * * 使用示例
 * 
 * ```
 * const value = getTagValue('YL1', 'YL');
 * console.log(value)
 * ```
 * 
 * @param {string} nodeId - 资产编号.
 * @param {string} tagId - 数据点编号.
 */
const getTagValue = (nodeId, tagId) => {
  return 123
}

/**
 * 设置组件的属性.
 * 
 * * 使用示例
 * 
 * ```
 * const value = getTagValue('YL1', 'YL');
 * console.log(value)
 * ```
 * 
 * @param {string} widgetId - 资产编号.
 * @param {string} attrName - 数据点编号.
 * @param {string} value - 数据点编号.
 */
const setWidgetAttr = (widgetId, attrName, value) => {
  return 123
}

const setWidgetEvent = {}

const fireWidgetEvent = {}

const Widgets = {

}

const Text1 = {
  name: '',
  onclick: (fn) => setWidgetEvent('Text1', 'click', fn),
  onsubmit: (fn) => {
    
  }
}